({
	/*checkout : function(component, event, helper) {
		var vx = component.get("v.callCheckout");
        //fire event from child and capture in parent
        $A.enqueueAction(vx);
	},
    
    closeModal : function(component, event, helper) {
        var vx = component.get("v.onClose");
        //fire event from child and capture in parent
        $A.enqueueAction(vx);
	}, */
    
})